function booWho(bool) {
    // What is the new fad diet for ghost developers? The Boolean.

    //typeof checks if it is a boolean
    return typeof bool === 'boolean';

    
  }
  
  console.log(booWho(null));
  